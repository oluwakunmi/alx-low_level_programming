#include <stdio.h>
/**
 * main - entry point with no argument
 *
 * Puts - prints character on the screen
 *
 * Return: 0 if successfull else 1 on error
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
